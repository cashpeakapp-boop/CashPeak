name: cashpeak
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.5.3
  google_mobile_ads: ^5.3.1

flutter:
  uses-material-design: true
  assets:
    - assets/cashpeak_logo.png
